# Release secrets — copy Config/Secrets.release.xcconfig.example and fill in locally
Config/Secrets.release.xcconfig

# Xcode user data
xcuserdata/
*.xcuserstate

# Derived data (local)
DerivedData/
build/
AppStoreScreenshots/

# macOS
.DS_Store

# ASC → Cursor review intake (local / Actions cache mirror)
.asc-review-intake-state.json
AuthKey_*.p8

# Persistent simulated-user SwiftData (local / Actions cache; do not commit)
living-users/
