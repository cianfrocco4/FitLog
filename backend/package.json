{
  "name": "fitlog-ai-proxy",
  "version": "1.0.0",
  "description": "Proxy for FitLog OpenAI requests; keeps API key on server.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
